# Coverage-expansion validation — 8 September 2026

- Public INCOIS ERDDAP catalogue: all 16 scientific products represented. Fifteen grid products expose all 86 source variables; exact time axes total 26,084 steps across products. All fifteen newest-time grid selections returned data, as did the Indian Argo table.
- Full backend suite: 70 passed in 99.04 s. After scientific-lock and immutable-source changes, catalogue/scale suite: 17 passed in 19.09 s. Final catalogue suite including failed-refresh preservation: 6 passed in 7.02 s. Existing dependency deprecation/binary-ABI warnings remain; no test failures.
- Frontend scientific suite: 23 passed in 260 ms, including exact region subsetting over multiple depths and frames. TypeScript and lint pass. Both portable Vite and Vinext Worker production builds pass. Existing chunk-size and future Vite JSON-import warnings remain.
- Zero-tolerance comparison of every bundled source-grid variable against original decoded NetCDF values/missing masks. Source checksums retained. Full-domain thermal frames preserve exact temperatures and observation counts. Baseline source files are preserved outside the request cache.
- Browser: all seven Ocean lab regions selected successfully on the production Worker. Monthly-change and local-observation filtering worked without console warnings/errors. The Arabian Sea API view returned bbox [50,-2,78,27], 13,950 selected native cells versus 129,600 in the full source frame, with original-source interpolation semantics intact.
- The 10-day variational-analysis source opens its full Indian Ocean volume in the production preview. Its error-field explanation preserves the published unit ambiguity instead of calling relative errors standard deviations. Explorer bounds use hemisphere-aware labels.
- Historical 3D request: 15 January 2001 monthly analysis registered and rendered through the standard volume endpoint. Single-time selection disables playback and avoids a zero-length slider.
- Historical Argo request: 24 October 2002, first archive day, returned float 2900226 cycle 1 A, 51 levels; 44 pass strict QC. The request starts at the first published timestamp (19:30:03Z), rather than an invalid midnight before coverage. Rejected pressure levels do not stretch the accepted-profile chart axis. Source SHA-256: 1935835476ae2f942f2d1ee229a47f693211dd58f2b423964f6b77fd25f91be6.
- Newest Argo snapshot: float 7902250 cycle 12 A on 23 April 2025, 91 raw levels passing the chart filter. Adjusted-channel values are absent and stay absent. Source SHA-256: b27e429d29787a26e6f9a2449771dc7cb0ed364a5c846ad6fcdac14b9ad15c76.
- Browser geography: global AMSR-E map aligns with Natural Earth coastline and uses the source 0–360° longitude convention with hemisphere labels. Missing cells remain dark. Native NetCDF exports remove display striding without changing selected time/depth/region.
- Mobile source browsing and Argo chart: 390 px viewport request yielded 375 px content width after scrollbar; document width equalled client width. Desktop at 1,265 px also had no overflow. Temporary viewport override reset after testing.
- Fixed during QA: Bay-only scene-coordinate text, fixed-size evidence-map viewport, south-hemisphere labels, stale regional probe indices during hot reload, native date-input events, first observation-day boundary, and single-frame playback controls.

The local application is available on port 3000 with its scientific API; port 3001 is the production Worker snapshot preview. Public upload and deployment were not attempted: the prior automatic approval review requires explicit authorization for source/data upload and publication to the existing public site. No live synchronization scheduler, restricted INCOIS feed, distributed failover, Docker execution or operational certification is claimed.

## Real-current completion

- Acquired and preserved FNMOC HYCOM ESPC-D V02 fixed-run velocity and scalar subsets: initialization 6 September 2026 12:00 UTC, valid times 7 September at 12:00, 15:00 and 18:00 UTC. All four fields share exact coordinates and initialization. The source subset has 93 × 123 horizontal cells and all 40 published depths, 0–5,000 m. Horizontal stride 12 yields approximately 0.96° longitude × 0.48° latitude spacing.
- Rejected the initial best-available combination because its velocity and scalar collections used different forecast initializations despite matching valid times. The final source requests explicitly select the same fixed run. Original NetCDF files and checksums are retained; `time_origin` template metadata is not used as the initialization.
- Current components and missing masks match original decoded source values exactly. At 5,000 m there are 3,960 finite current samples across three source frames. Native-field NetCDF round-trip checks preserve the selected component, source depth levels, units and mask. Potential temperature uses GSW with matching source salinity, depth and position.
- Backend run: 74 tests passed and one new assertion failed because it expected `m/s` instead of the existing CF export spelling `m s-1`. After correcting that assertion, all four current-specific tests passed in 2.17 s. The other 71 tests were unchanged. A redundant full rerun was stopped under host memory pressure; a single clean 75-test run is not claimed. The dependency's existing NumPy binary-ABI warning remains.
- Final frontend checks: TypeScript and lint pass; all 25 scientific tests pass in 291 ms. Tests include analytic velocity signs and depth shear, missing values, coordinate/time mismatch rejection, fixed-depth multi-layer RK4 paths, and existing scalar/section/contour calculations. Both portable Vite and Vinext Worker production builds pass. Existing chunk-size and future Vite JSON-import warnings remain.
- Browser with the scientific API: full column reports 832 arrows at 12 display depths. Selecting 5,000 m reports 80 valid arrows; the central probe correctly reports Missing at that depth. Every-display-depth mode reports 747 arrows over the balanced grid's 36 depths. Source files retain 40 native depth levels; these counts are not conflated.
- Verified equal-length and speed-scaled glyph modes, temperature slices, temperature isosurface geometry, salinity intersecting sections and Arabian Sea subsetting. The regional view reports 852 arrows over 36 display depths. The depth evidence panel updates speed, signed components, direction towards, valid time and forecast lead.
- An initial combined dense-vector/streamline/playback stress test became unresponsive under low host memory. Fixed animation scheduling to leave time between renders, reduce volume image sampling while flow animation is active, skip hidden-document animation, and wait for matching scalar/u/v frames before advancing time or exporting evidence. The retest advanced from 12:00 to 18:00 with 747 arrows and 39 streamlines, then responded to Pause and overlay controls. This is a functional responsiveness check, not a hardware-independent frame-rate benchmark.
- Browser console: no warnings or errors in the final local and production retests. The evidence-export action executes. At a 390 px viewport, content and scroll width both equal 375 px; mobile controls expose current coverage, appearance, layer count and probe selectors. The temporary viewport override was reset.
- Production Worker on port 3001 identifies itself as a bundled snapshot and loads all three current frames without the Python API. The depth evidence correctly shows forecast leads +24, +27 and +30 hours. Original source-current NetCDF is linked from the evidence panel.
- Local frontend commit: `7a0b24a619415ca6d888cf41a2bc95cdfaa9d4cd`. Local archive `tmp/ocean3d-site-real-currents.tar.gz`: 31,608,697 bytes, SHA-256 `c2fea7d89dba55a02eb52c5a1b81008f86d036aa6c1d36b20720322bd8ad7172`. Verified 24 current/scalar display packets, both original NetCDF checksums, required Worker/hosting files, retained INCOIS model entries and exclusion of secrets, dependency directories and backend data. Largest asset is 10,987,036 bytes, below 25 MiB. No source upload or publication occurred.

This closes the real horizontal-current data and full-column rendering gap. It does not add vertical velocity, continuous live synchronization, time-evolving drift prediction, institutional acceptance or public deployment. See [current-data details](CURRENTS.md) and [remaining operational boundaries](REQUIREMENTS_MATRIX.md).

## Instrument overlay completion

- Shared Instruments/Source/Coverage gaps/Physics inspector now works with every model. The real collection has 12 profiles: two reference Argo casts, four IMOS Seaglider casts, four GO-SHIP CTD casts and two BGC-Argo casts, including INCOIS float 2902273. Instrument markers use native geographic coordinates; glider paths retain per-sample positions, times and depths. Nearby labels remain separately selectable through anchored leader lines.
- Preserved original source NetCDF, bounded native subsets, source QC/units and SHA-256 provenance. Exact source-subset equality and independent GSW calculations pass. Native IMOS and CCHDO upload adapters register four casts from each subset and persist NetCDF files. The checksum-verified normalized profile cache loads in 0.188 s on this host, compared with about 22 s for repeated native metadata decoding; this is collection loading only.
- Eight instrument-specific tests pass across targeted runs, covering source fidelity, pressure/depth and temperature conversion, WOCE/IMOS/Argo QC, moving-sample model queries, unsupported model variables, native uploads, unit rejection and cache tampering. The broader API/science/extension run passed 37 tests with one incorrectly shaped new test double; after fixing that test double, all seven original instrument tests passed. The subsequent cache test and expanded API/native-upload test also pass. A single full backend-suite rerun is not claimed.
- All 28 frontend scientific tests pass; type checking and lint pass. Final portable Vite and Vinext Worker builds pass. Existing chunk-size and future Vite JSON-import warnings remain. No dependency or stack change was needed.
- Scientific-API browser checks: selected real Glider, CTD and BGC markers on INCOIS; verified cast start/end timestamps, native depth charts and QC exclusions. Selecting chlorophyll remains independent of the HYCOM rendered variable. The INCOIS BGC cast has zero strict chlorophyll readings and 58 accepted readings with the explicit QC-2 policy; 996 missing/rejected rows remain excluded. Source/Instruments switching preserves the selected profile and variable. CSV download action succeeds; automated export tests verify native rows, timestamps, positions and source flags.
- At a 390 × 844 viewport, document client width and scroll width both equal 375 px. The mobile review exposed a fixed-height tab conflict; the final evidence tab grid is 72 px tall with four 32 px controls. No horizontal document overflow or alert was present. Temporary viewport override was reset.
- Standalone production Worker on port 3001 identifies its bundled snapshot and renders the real IMOS cast with 543 accepted temperature samples, native timestamps and a profile chart without the Python API. AOML BGC chlorophyll retains 504 strict-QC values; no model values or residual metrics are fabricated. Model comparisons that are not bundled are explicitly identified. The count label reads “Observed without a model match,” avoiding an unsupported claim that model support was evaluated.
- Retained reference comparison: the Real-data validation journey still shows 1,001/1,001 matched Argo levels, RMSE 0.66 °C and mean bias +0.11 °C in the standalone build. Source/Instruments navigation preserves the independent observation variable. Final API and production browser consoles contain no warnings or errors; final screenshot inspection confirms the 3D markers, leader lines and depth chart.
- Isolated local frontend commit: `9e33dba66d789e0d56df98c8db939ac0a8da14f3`. Local release archive `tmp/ocean3d-site-real-instruments.tar.gz`: 32,964,764 bytes; SHA-256 `2584eb84c51bf0b52a778f10fc796abe30081ef7023afb4a06f58684c682f133`. Verified all 18 profile packets (12 real and six separately labelled analytic examples), four native instrument-file hashes, all 24 retained current/scalar display packets plus source metadata, both original HYCOM files, required Worker/hosting files, absence of dependency/secret/backend directories, and maximum asset size 10,987,036 bytes. The archive was inspected without extraction. No source upload, version save or publication occurred.

The supported instrument overlay requirement is implemented and verified locally for these formats and cases. These observations are historical snapshots, not a live feed. Missing time, location or variable support never produces invented model matches. See [instrument data, methods and limitations](INSTRUMENT_OVERLAYS.md). Public upload/publication remains blocked by the prior automatic approval rejection pending explicit authorization.

## Multi-format ingestion completion

- Added automatic signature/metadata detection for CF model grids, Argo, CF profiles, IMOS glider and CCHDO CTD files. Real retained source files exercise the detector. Specialized native contracts take precedence over the generic CF profile adapter; ambiguous plugin detectors reject and allow an explicit selection.
- Delimited parsing now supports long/wide UTF-8 tables, BOM/comment preambles, blank lines, comma/tab/semicolon/pipe/whitespace, explicit variable aliases and documented unit conversion. Per-sample positions/times, original values/units/depths and QC are retained. Invalid units, malformed schemas, non-finite values, negative errors and ambiguous definitions reject with record-level messages. Missing QC stays unknown; omitted built-in units are explicitly reported as canonical-unit assumptions.
- One parse/validation contract serves preview and import. `/api/import/inspect` closes/removes temporary files and leaves the catalog untouched. `/api/import` retains original bytes, detected adapter, SHA-256 and the validation report in an atomic manifest. Automatic/explicit requests deduplicate the same resolved source; wrong idempotency keys and failed parsers roll back. Restart restoration rejects a checksum mismatch. Preview performs a bounded lazy model decode, not a full validation scan of every grid cell.
- Extended rectilinear normalization to one-dimensional auxiliary coordinates on differently named dimensions. A packed int16/scale-factor NetCDF fixture preserves decoded source values and masks while swapping dimensions, without regridding. Explicit projected-coordinate units reject.
- The frontend reads its format options from the registry. Trusted custom NetCDF adapter testing proves metadata detection, catalog discovery, parsing and a persisted `.nc` file without API/frontend branches. Variable-manifest oxygen works in text and model normalization. Administrator module registration rolls back on failure. No new dependency or stack change was needed.
- Automated inbox receipts are atomic and distinguish the requested adapter as well as the content hash. Source files remain intact; failed runs return a nonzero command status. No new scheduler or live feed was activated.
- Backend regression run: **59 passed in 239.94 s**, covering ingestion, API, scientific normalization/comparison, CF/extensions and the real instrument adapters. Final focused suite after checksum-restoration/inbox/plugin-loading changes: **22 passed in 16.42 s**. These runs overlap; they are not 81 distinct tests. Existing Starlette/anyio deprecations and the NumPy binary-ABI warning remain.
- Frontend: **28 scientific tests passed in 337 ms**; final TypeScript and lint pass. Portable Vite and Vinext Worker production builds pass with existing chunk-size/future JSON-import warnings. An old API response lacking format metadata exposed a UI error during the transition; the component now reports unavailable discovery instead of dereferencing a missing array. Final browser reloads and interaction checks have clean warning/error consoles.
- Browser on port 3000: wide CSV preview reports one profile, nine values, three canonical variables, seven good readings, one probably-good reading and one missing value. Adding it opens the synthetic profile and displays converted temperatures 28.2, 24.0 and 16.0 °C. An invalid-unit file displays `Table record 2` and offers no add action. Selecting a valid file recovers normally.
- NetCDF browser preview reports two times, a 4 × 4 × 6 spatial grid, 0–208.33 m depth and five variables. Adding it opens the imported synthetic model with all five variable controls. Report-download action succeeds. At a 390 × 844 viewport, client and document scroll width both equal 375 px; the 297.6 px report and wrapped buttons remain usable. Temporary viewport override was reset.
- Standalone Worker on port 3001 loads the new interface, clearly asks for the scientific API and disables uploads when disconnected. It does not simulate successful ingestion in browser storage. Its final console contains no warnings or errors.
- Isolated local frontend commit: `a26ab5b6de124cb7115a4809281c102561c8c83c`. Packaged archive `tmp/ocean3d-site-ingestion.tar.gz`: 32,963,802 bytes, SHA-256 `11fcb840e1a1dc05aa24c770a3d75c41957b06d1de6f9f3dc1869f52f0e37b3e`. Required Worker/hosting files, the new wide-table example, all 18 retained observation packets and four instrument-source checksums pass archive verification. Largest asset remains 10,987,036 bytes. No source upload, version save or publication occurred; prior publication approval remains pending.

See [supported ingestion contracts and extension instructions](INGESTION.md). Arbitrary NetCDF/vendor layouts, all CF geometry types, curvilinear/sigma conversion and a general CF compliance certification are not claimed. The implementation completes the requested multi-format pipeline for its documented inputs and provides explicit extension points for additional sources.

## Colorbar and variable-control completion

The audit found opacity floors of 35% on vertical sections and 15% on isosurfaces, no independent overlay alpha, range inputs that rejected intermediate edits, and unsafe handling of invalid logarithmic colors. These gaps are fixed. Scalar lookup and legend ticks now share a tested color module; log display encoding masks nonpositive values without mutating native data. Field, instrument, vector and streamline opacities independently span 0–100%. The range editor validates two limits together, supports scientific notation, and provides explicit fit/reset actions. Sliders now expose their names on the focusable inputs.

All 34 frontend scientific tests passed in 442 ms; final TypeScript and lint pass. Both production builds pass with the existing chunk-size/future Vite-import warnings. No backend or dependency changes were made. Browser tests covered all palettes, variable changes, logarithmic signed-current masking, color limits across time changes, 0% versus full visibility, 1% isosurface alpha, 1×/600× exaggeration and saved views. The production Worker restored field/instrument/vector/streamline alpha 11/69/89/43% and exaggeration 401× after reload. Both final browser warning/error logs were empty. Mobile 390×844 review measured 375 px client and scroll width, with one canvas; the viewport override was restored. See [complete control semantics and verification](COLOR_CONTROLS.md).

Isolated frontend commit: `7f3c0312634721dfe0565af8749135e3a582724a`. Local archive `tmp/ocean3d-site-color-controls.tar.gz`: 32,970,573 bytes, 376 entries, SHA-256 `1f2536a4de8c22472a6f3625254f16705f1e25d2909a1cddf094c62f00dc6d75`. Archive verification found the required Worker/hosting files and new control code, no environment/dependency/Git directories, and a largest asset of 10,987,036 bytes. Public upload/publication remains pending the earlier approval; no external upload or deployment occurred.

## Web-based architecture completion

React/VTK and FastAPI/xarray were retained. REST fulfills the stated REST/OPeNDAP alternative; no OPeNDAP server was added. The portable frontend now requires a healthy, ready scientific API and valid nonempty catalog before connecting. It exposes authentication, startup and connection errors with a retry action. A disconnected institutional deployment cannot silently substitute the bundled scientific catalog. Separately labelled Ocean lab/source snapshots remain accessible.

Added `python -m backend.server`, which validates one writable worker or 1–8 read-only workers and bounded request settings. Read-only mode rejects uploads and upstream acquisitions before parsing, does not create an import directory, and retains scientific reads and exports. `/api/ready` detects failed import restoration independently of liveness; `/api/runtime` exposes startup catalog identity and process-local settings. OpenAPI and optional Swagger UI now live under `/api` for proxy routing. Compose/nginx gained explicit resource settings, health checks, upstream keep-alive, error preservation and an immutable read-only overlay. [Complete deployment contract](DEPLOYMENT.md).

- **Backend: 29 tests passed in 50.94 s** in the final deployment/API/scaling run. Tests cover unsafe worker configuration, read-only mutation and remote-IO rejection, usable scientific queries, failed restoration, authentication, admission responsiveness, binary frames and existing API/scale behavior. An older import-responsiveness fixture initially failed the stricter ingestion contract; it was corrected to supply valid coordinates, timestamp and sample metadata. The final run is clean. Existing Starlette/anyio deprecations and the NumPy binary-ABI warning remain.
- **Frontend: 37 tests passed in 271 ms**, including required-API failure/authentication, readiness/catalog checks, standalone behavior and read-only capabilities. Type checking and lint pass. Both final portable Vite and Vinext/Worker builds pass; existing chunk-size and future JSON-import warnings remain. No dependency changes were made.
- **Actual multi-worker check:** two Uvicorn workers served nine models and 23 profiles through the portable frontend proxy on port 3002. The probe identified both runtime processes and actual frame-serving processes and verified matching startup revisions and frame hashes. Upload/import-preview/remote-open attempts returned 403. [Raw read-only result](benchmarks/deployment-readonly-2026-09-08.json). The restored single-worker writable API passed the same-origin contract checks on port 3000; [raw writable result](benchmarks/deployment-writable-2026-09-08.json). This uses a small synthetic scalar consistency fixture and is not a load or forecast-skill benchmark.
- **Browser:** read-only mode displayed its badge and administrator import message; uploads, source date/depth changes and remote acquisition were disabled. The real HYCOM/Argo volume and depth slice rendered, and selecting the real Argo marker retained the native depth chart and 1,001/1,001 matched levels with the existing 0.66°C rounded RMSE. The inspected final warning/error log was empty.
- **Outage/recovery:** stopping only the isolated test API produced a visible HTTP 502 connection alert with Retry connection and zero scientific-model Explore buttons. Restarting it and clicking Retry connection restored all nine models and read-only permissions. The normal port-3000 service was then restored with imports enabled and no visible alert. Temporary read-only/portable helpers were stopped.
- **Standalone production:** port 3001 still labels its HYCOM dataset as a bundled snapshot, renders without the scientific service and disables imports. Inspected alerts and warning/error logs were empty. The main review tab was returned to the scientific app on port 3000.
- **Container configuration:** `docker compose config --quiet` and the combined writable/read-only/archive overlay configuration pass. The Docker Linux engine remains unavailable, so image build, nginx execution and read-only Linux container filesystem acceptance were not run. No institutional TLS/identity, sustained user load or cross-host failover acceptance is claimed.

Isolated frontend commit: `41908701337b1db9b142d598b92e01612ce8a1e3`. Local build archive `tmp/ocean3d-site-architecture.tar.gz`: 32,967,706 bytes, 373 entries, SHA-256 `b00444f238435f12b7ae4132f135e92b5a1c8050cd4dd6eac7a10ce3eadf314a`. Verification confirms the Worker/hosting/catalog files and compiled deployment UI, no environment/dependency/Git directories or symlinks, and a largest asset of 10,987,036 bytes. This archive contains compiled frontend assets; the separate backend and deployment configurations remain in the local repository. No source upload, version save or publication occurred; the prior external-upload approval remains pending.

## Extensible sensors, model readers and derived products

Added API-version-1 extension packs with atomic registry rollback, installed-extension discovery, model-reader registration, sensor geometry contracts and versioned derived/ML output definitions. Existing variable and observation extensions remain supported. Canonical variable IDs/units are accepted alongside vendor aliases, including source-specific quantities without a CF standard name. Imported manifests pin the adapter pack version; derived outputs require explicit product version and lineage and cannot bypass validation through the generic model adapter.

Mooring, ADCP and HF-radar adapters accept small documented canonical tables with timestamps, coordinates, units and QC. Moorings retain fixed depth and use a time axis; ADCP records become separate timestamped depth profiles; HF radar remains nominal surface total currents. The browser chooses the appropriate inspector. Station freshness and coverage use actual readings rather than the station's first timestamp or a continuous deployment span. Missing/QC-rejected samples break chart lines; both model and observed markers remain visible for isolated values.

- **Backend:** the existing extension/deployment run passed 19 tests; ingestion/API passed 28. The first new contract run passed 13 tests, followed by two focused sensor/coverage tests (one overlap). Final canonical-variable/product-gate changes were checked with scientific normalization and extension regressions: 26 passed initially, with one new multipart fixture failing because xarray returned a memoryview. Converting that fixture to bytes fixed the transport; its rerun passed (1 test, 8.24 s). All targeted tests have passed; these overlapping runs are not a full-repository test claim. Existing Starlette/anyio deprecations and NumPy ABI warning remain.
- **Contracts exercised:** exact sensor geometry, paired earth-referenced components, unit conversion, QC exclusion, per-sample model timestamps, malformed positions/depths/duplicates, atomic pack rollback, unsupported pack API, custom model readers, canonical variable round-trip, required product lineage, invalid/empty product IDs, version mismatch, preserved native arrays/masks and exported lineage. No trained model or raw beam/radial decoder is exercised.
- **Frontend:** all **40 scientific tests passed in 430 ms**; final TypeScript and lint pass. Portable Vite and Vinext/Worker production builds pass. Existing chunk-size and future JSON-import warnings remain. Dependencies and stack are unchanged.
- **Browser imports:** mooring preview/import produces three temperature readings at 50 m (28, 27.5, 27.2 °C) and three separately timed model matches. ADCP preview produces three profiles, 18 variable samples and depths 20/50/100 m; the 06:00 northward component shows 0/3 eligible with all three QC-bad values excluded. HF-radar preview produces six variable samples at nominal 0 m; its northward inspector retains the two good timestamps and leaves the rejected middle sample unconnected, with 2/3 matches. Inspected charts display actual times and model/observation points. These numbers are synthetic integration checks, not forecast skill.
- **Derived-field browser workflow:** the analytic ML-output fixture previews its registered variable and complete declared lineage, imports as a model, opens the Source inspector, and renders in volume and horizontal slice modes across time. The source panel clearly states that no ML model was trained/executed. Input/artifact hashes, training-data description and inference domain remain inspectable. The current desktop layout has one canvas and equal document/client width (1265 px); no new mobile viewport test was run for this extension. Final inspected local warning/error logs were empty.
- **Persistence:** restarting the ordinary API restores 10 models and 28 observation records, including all five new sensor records and the derived field, with **zero restoration errors**. Three extension packs, sensor timestamps and complete product lineage remain present. [Restoration result](benchmarks/extensions-restoration-2026-09-08.json).
- **Standalone production:** the rebuilt port-3001 app loads the extension panel, asks for the scientific API, and disables imports. Its inspected warning/error log is empty. The review tab was returned to the API-connected port-3000 app. No live sensor connection or inference scheduler was created.

Isolated frontend commit: `ee578eaf1438b6751b5298f1769b8797a8dda787`. Local archive `tmp/ocean3d-site-extensions.tar.gz`: 32,975,555 bytes, 381 entries, SHA-256 `0cd7d439a1a384bd1e4608b616c5e706f5db60768293dc5f77c7cdad68704e84`. Verification confirms required hosting/Worker/catalog files, compiled extension/chart/provenance UI and all four example files byte-for-byte; no environment/dependency/Git directories or symlinks. Largest retained asset is 10,987,036 bytes. The separate backend remains local. No external source upload or publication occurred; the earlier approval boundary remains unchanged.

See [the extension guide](EXTENSIBILITY.md) for installation, schema details and boundaries. Plugin-style design is implemented for the documented grids and geometries. Arbitrary vendor files, live institutional sensors, dense radar-grid rendering, ML training/prediction quality and target infrastructure acceptance remain distinct integration tasks.

## Public INCOIS synchronization, evaluation and HTTP load

Implemented scheduled acquisition from public INCOIS ERDDAP using the existing React/VTK/FastAPI/xarray stack. Writable start.ps1/Compose defaults to six-hour checks; direct Python launches opt in and read-only replicas disable it. The source library now reads the active API catalogue, pins requests to metadata/axis revisions, reports source dates and failures, supports a manual check and explicitly loads a new generation. No restricted source credentials or HYCOM refresh are configured.

- **Actual public source cycles:** the initial check acquired all 16 products in 35.31 s. The browser's Check INCOIS now action then completed another full check in 31.71 s and offered Load updated source catalogue. Four full-basin models retain three source frames and 24 native depths. The newest bounded Indian Argo selection contains float 7902250, cycle 12, ascending cast, 23 April 2025 at 13:28 UTC, with 91 raw levels. [Initial acquisition/restoration](benchmarks/incois-sync-2026-09-08.json), [browser-triggered acquisition](benchmarks/incois-sync-browser-2026-09-08.json).
- **Freshness:** the 10-day analyses end 30 July 2026 and monthly analyses end 15 July 2026; public Argo ends 23 April 2025. Other surface products are historical too. Successful polling is not a current operational forecast or a full archive mirror. Surface winds/diagnostics retain their original meaning.
- **Recovery:** immutable generation files have SHA-256 manifests; pointer-write failure leaves serving data unchanged, publication rejection restores the prior pointer, and individual source failures retain explicitly labelled previous packets. Expired axes fail instead of using a different catalogue's indices. Interruptions prevent publication; persistent-status failures remain visible without killing the scheduler. Two actual API restarts restored 14 models and 29 observation records with zero restoration errors and preserved the scheduled check rather than downloading again. [Final restoration](benchmarks/incois-sync-final-restoration-2026-09-08.json).
- **Backend checks:** the first source/synchronization/deployment run passed 25 tests, and the evaluation/API/ingestion run passed 30. The final synchronization/evaluation suite passed **14 tests in 58.21 s**, including pointer-write and publication failures, metadata and per-product fallback, corruption, revision retention, schedule restoration, shutdown, read-only permissions, raw Argo QC and status-disk failures. These overlapping runs are not a full-suite or additive test-count claim. Existing Starlette/anyio deprecations and NumPy binary-ABI warnings remain.
- **Frontend checks:** **41 scientific tests passed**, including evaluation timing, input rejection and unscored output. Final TypeScript and lint pass. Both portable Vite and Vinext Worker production builds pass. Extracting the shared selector restores the source library's lazy loading; the new ineffective-dynamic-import warning is resolved. Existing large-chunk/future JSON-import warnings and build plugin-timing notices remain. No dependency or stack change was needed.
- **API browser:** manual refresh transitions from queued to completed; catalogue adoption opens the checked generation. Open full water column now selects the full 5–2,000 m extent rather than the upper-500-m display window. The source inspector opens immediately and clearing a profile clears its old error. The synchronized Argo marker displays 91 accepted potential-temperature values, its original source identity, timestamp and raw-channel/GSW/QC disclosures. INCOIS's unspecified temperature convention produces zero model matches and no fabricated RMSE. Full-depth volume, geographic context and native depth chart were visually inspected. Desktop client/scroll width both equal 1265 px, with one canvas and no inspected warning/error logs. No new mobile viewport or GPU throughput test is claimed for this change.
- **Evaluation browser:** all three fixed tasks open the correct model/frame; empty responses reject. One explicitly labelled developer-practice response was recorded and the export action succeeds. Cancelled current and source-interpretation tasks do not increase the recorded-trial count. No actual forecaster participant or usability benefit is claimed. The assessor protocol requires source verification, pseudonyms, counterbalancing and independent scoring; the paired analyzer excludes practice, unscored and unmatched trials.
- **Bounded HTTP load:** 1/2/4 concurrent clients made 588/922/1,624 successful requests in approximately ten seconds per stage. There were no HTTP failures, capacity rejections, inconsistent frame hashes or failed readiness probes. Four-client p95 latency was 50.47 ms; maximum readiness latency was 213.33 ms. This is a small three-frame synthetic workload including cache warming through Vite's same-origin proxy, not GPU/native-archive/production acceptance. [Raw load result](benchmarks/http-load-2026-09-08.json).
- **Standalone production:** the rebuilt port-3001 preview states that its bundled snapshot cannot synchronize without the scientific API. Its timed reference task opens the real HYCOM/Argo case, retaining 1,001 matched levels and 0.66 °C rounded RMSE. The inspected warning/error log is empty. The review tab returns to the API-connected app on port 3000.
- **Deployment:** writable and combined read-only Compose configurations validate. A fresh docker-info check still cannot find the Docker Desktop Linux-engine pipe, so container/image/nginx execution remains unverified. Institutional authentication/TLS, representative long-duration concurrency, operational feeds and actual forecaster evaluation require the target environment and participants.

Isolated frontend commit: `18fb4f540f0b0ab58f633bc8a1fe504189458c82`. Local archive `tmp/ocean3d-site-synchronization.tar.gz`: **32,912,649 bytes**, **376 entries**, SHA-256 `62c933906330ea6f7c567c4a283e68cf5214cd6ee89cf2be1c7ac864adfc4a3b`. The Sites build-preparation helper staged the distribution in a checked workspace directory; it was archived locally without temporary recursive cleanup. Verification confirms required Worker/hosting files, compiled synchronization/evaluation/dossier code, all **230 public files byte-for-byte**, no environment/dependency/Git directories or symlinks, and a largest asset of 10,987,036 bytes. Backend source and deployment configuration remain in the local repository. No external upload, version save or publication occurred; the earlier external-upload approval boundary remains unchanged.

See [synchronization](SYNCHRONIZATION.md), [forecaster evaluation](FORECASTER_EVALUATION.md) and [requirement coverage](REQUIREMENTS_MATRIX.md). Implemented software and executed local checks are distinguished from unavailable operational feeds, untested target infrastructure and incomplete human validation.
